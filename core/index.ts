import assets = require('@molecule-driver/assets');
import client = require('@molecule-driver/client');
import server = require('@molecule-driver/server');

const core = {
  assets,
  client,
  server
}

export = core;
